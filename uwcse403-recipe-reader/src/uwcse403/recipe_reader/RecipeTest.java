/**
 * Tests the recipe class to make sure the ingredients
 * and instructions are being set up and accessed properly.
 * Black Box Test
 * Jonathan Ramaswamy (ramasj@cs.washington.edu)
 */
package uwcse403.recipe_reader;

import junit.framework.TestCase;

public class RecipeTest extends TestCase {

	/*public void testIngredients() {
		Recipe r = new Recipe("Very Delicious Hamburgers");
		assertEquals("Beef", r.getIngredients().get(0));
		assertEquals("Lettuce", r.getIngredients().get(3));
	}
	
	public void testInstructions() {
		Recipe r = new Recipe("Mediocre Hamburgers");
		assertEquals("Put the beef on the bun", r.getInstructions().get(0));
		assertEquals("Eat", r.getInstructions().get(0));
	}
	
	public void testStepNumbers() {
		Recipe r = new Recipe("Test");
		assertEquals(0, r.getCurrentStepNumber());
		r.nextStep();
		assertEquals(1, r.getCurrentStepNumber());
		r.goToStep(3);
		assertEquals(2, r.getCurrentStepNumber());
		r.previousStep();
		assertEquals(1, r.getCurrentStepNumber());
	}
	
	public void testSteps() {
		Recipe r = new Recipe("Test");
		assertEquals("Put the beef on the bun", r.getCurrentStep());
		r.nextStep();
		assertEquals("Put the rest on", r.getCurrentStep());
		r.goToStep(3);
		assertEquals("Eat", r.getCurrentStep());
		r.previousStep();
		assertEquals("Put the rest on", r.getCurrentStep());
	}*/
}